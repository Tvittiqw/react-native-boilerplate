export type LoginFormValuesType = {
    email: string
    password: string
}

export type SignUpFormValuesType = {
    firstName: string
    lastName: string
    id: string
    email: string
    password: string
    confirmPassword: string
}