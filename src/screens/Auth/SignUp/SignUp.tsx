import React, {FC} from 'react';
import {SafeAreaView, Text} from "react-native";

const SignUp: FC = () => {
    return (
        <SafeAreaView>
            <Text>SignUp</Text>
        </SafeAreaView>
    );
};

export default SignUp;