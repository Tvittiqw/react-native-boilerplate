import React, {FC} from 'react';
import {SafeAreaView, Text} from "react-native";

const Login: FC = () => {
    return (
        <SafeAreaView>
            <Text>Login</Text>
        </SafeAreaView>
    );
};

export default Login;